import { ClassToggleDirective } from './class-toggle.directive';

describe('ClassToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassToggleDirective();
    expect(directive).toBeTruthy();
  });
});
